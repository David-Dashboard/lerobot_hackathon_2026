"""coord-grasp: self-calibrating, coordinate-only grasp experiment (separate branch).

Front-end produces object coordinates (from depth / markers) that a pixel-free,
goal-conditioned policy consumes. Starts here with the OAK-D-PRO RGB wrapper.
"""
